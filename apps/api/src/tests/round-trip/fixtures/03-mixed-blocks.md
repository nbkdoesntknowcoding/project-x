# Mixed blocks

Plain paragraph text introducing the section.

> A blockquote with two short sentences. The second one is here.

A second paragraph between block types.

```typescript
function hello(name: string): string {
  return `Hello, ${name}`;
}
```

- bullet one
- bullet two

Final paragraph after the list.
