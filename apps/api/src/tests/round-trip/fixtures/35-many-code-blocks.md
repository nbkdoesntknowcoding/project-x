# Many code blocks

Block one (TS):

```typescript
const a = 1;
```

Block two (Python):

```python
b = 2
```

Block three (Rust):

```rust
let c: i32 = 3;
```

Block four (Bash):

```bash
echo "four"
```

Block five (SQL):

```sql
SELECT 5 AS five;
```

Block six (JSON):

```json
{ "value": 6 }
```

Block seven (YAML):

```yaml
key: value
```
