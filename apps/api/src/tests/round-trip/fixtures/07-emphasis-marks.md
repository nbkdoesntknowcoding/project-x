# Emphasis marks

A paragraph mixing **bold**, *italic*, ***bold italic***, and `inline code` markers.

Another paragraph with **bold spanning multiple words** and *italic spanning multiple words too*.

A run of `code spans` and **bold** sitting next to each other in one line.
