# Inline math

Energy and momentum live inline: $E = mc^2$ and $p = mv$.

Greek symbols inline: $\alpha + \beta = \gamma$ and the integral symbol $\int$ on its own.

Subscripts and superscripts inline: $x_1^2 + x_2^2 = r^2$ in the same paragraph as some prose.
